package com.improve_future.json_map

class JsonMapTest {
    fun testJsonMap() {
        var a = listOf("a")
        when(a) {
            L -> {
                println(a)
            }
        }
    }
}